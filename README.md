Girişimci Türk Demo Çalışma

## Monorepo Yapısı

- `apps/api` → Node.js + Express (mock/in-memory repo)
- `apps/web` → Vite + React (minimal UI)

## Hızlı Çalıştırma

### 1) API

```bash
cd apps/api
pnpm install
npm run start
```

### 2) WEB

```bash
cd apps/web
pnpm install
pnpm dev
```
